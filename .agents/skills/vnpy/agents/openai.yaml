interface:
  display_name: "Vnpy"
  short_description: "Repo-specific patterns and workflows for vnpy"
  default_prompt: "Use the vnpy repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true